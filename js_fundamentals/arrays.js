// // create an array
// let colours = ["green", "orange", "red"]
// // length
// console.log(colours.length)
// //adding/removing (to the end/begininng of array)
// let addColourToEnd = colours.push("pink") //adds to end
// let removeEndColour = colours.pop() //removes end item
// let addColourToStart = colours.unshift("black") //adds to start
// let removeFirstColour = colours.shift() // removes first item

// // accesing an element (index?)
// let secondValue = colours[0]
// console.log(colours[0])

// console.log(colours[2])

// // look up splice method
// // colours.splice(1, 0, "blue")
// colours.splice(2, 1, "yellow")

// console.log(colours);

// combining arrays
// deleting elements from arrays


// any useful methods (to get sum?)
//sorting
// filtering
// looping/iterating

var sports = ['football', 'tennis', 'rugby', 'snooker', 'handball']

for(var sport of sports) {
    console.log(sport.toUpperCase());
};

for(var i = 0; i < sports.length; i++) {
    console.log(sports[i].toUpperCase());
};